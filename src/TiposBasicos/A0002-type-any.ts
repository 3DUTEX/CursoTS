/* 
Exemplo de tipo any:

Deve ser usado somente em último caso
  function showMessage(msg) {
    TS infere any para msg, pois não foi definido nenhum tipo
      return msg;
  }
*/

console.log();

export default 1;
